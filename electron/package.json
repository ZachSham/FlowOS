{
  "name": "@flowos/electron",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test:native-actions": "npm run build --workspace @flowos/shared && npm run build --workspace @flowos/db && npm run build && node dist/actions/testNativeActions.js",
    "test:window-editor": "npm run build --workspace @flowos/shared && npm run build --workspace @flowos/db && npm run build && node dist/actions/testSwiftWindowEditor.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@flowos/db": "*",
    "@flowos/shared": "*",
    "ws": "^8.18.2"
  }
}
