{
  "name": "@flowos/extension-chrome",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/background.ts --bundle --format=esm --outfile=dist/background.js && esbuild src/contentScript.ts --bundle --format=esm --outfile=dist/contentScript.js && node scripts/copy-manifest.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.323",
    "esbuild": "^0.25.3",
    "typescript": "^5.8.3"
  }
}
